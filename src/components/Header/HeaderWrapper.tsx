"use client";

import { FC } from "react";
// import { usePathname } from "next/navigation";
import { HeaderMainPage } from "@/components";

const HeaderWrapper: FC = () => {
  // const path = usePathname();
  return <HeaderMainPage />;
};

export default HeaderWrapper;
