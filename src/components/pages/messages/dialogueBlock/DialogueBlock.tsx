import { Box } from "@mui/material";
import style from "./DialogueBlock.module.scss";

const DialogueBlock = () => {
  return <Box className={style.blockWrapper}>messagesBlock</Box>;
};

export default DialogueBlock;
