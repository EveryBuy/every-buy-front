/**
 * Filter UI return visual jsx element
 *
 * TO DO: MAKE FILTER WORK
 * @returns {JSX.Element} - View UI of filter
 */

export function CatalogyFilter(): JSX.Element {
  return <form>some filter</form>;
}
